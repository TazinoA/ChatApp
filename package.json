{
  "name": "chatapp",
  "version": "1.0.0",
  "main": "backend/servers/server.js",
  "scripts": {
    "build": "npm install --prefix backend && npm run build --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "ci": "npm ci --prefix backend && npm ci --prefix frontend",
    "start": "npm run start --prefix backend",
    "test": "npm run test --prefix backend"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/TazinoA/ChatApp.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/TazinoA/ChatApp/issues"
  },
  "homepage": "https://github.com/TazinoA/ChatApp#readme",
  "description": "Full-stack real-time chat application with Socket.IO, Express, React, and PostgreSQL"
}
